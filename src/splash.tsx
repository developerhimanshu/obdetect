import {StyleSheet, View} from 'react-native'
import { Text } from 'react-native-paper'

const Splash = ()=>{
    return (
        <View>
            <Text style = {styles.textStyles} >OB<br/>DETECT</Text>
        </View>
    )
}
const styles = StyleSheet.create({
    textStyles:{
        
    }
})

export Default Splash;